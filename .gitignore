# ── Unity generated ──────────────────────────────────────────────────────────
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
# Packaged player builds — CI rebuilds these; never commit build artifacts.
WindowsClient/
[Ll]ogs/
[Mm]emoryCaptures/
[Uu]serSettings/

# ── Unity asset pipeline cache ────────────────────────────────────────────────
*.pidb
*.pdb
*.mdb
*.pidb.meta
*.pdb.meta
*.mdb.meta

# ── OS clutter ────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store.meta
Thumbs.db
desktop.ini

# ── IDE & editors ─────────────────────────────────────────────────────────────
.idea/
.vscode/
*.suo
*.user
*.userprefs
*.csproj
*.sln
*.slnx
*.unityproj

# ── Cowork / local session files ──────────────────────────────────────────────
.claude/

# ── Build artifacts ───────────────────────────────────────────────────────────
*.apk
*.aab
*.unitypackage
*.app
*.apk.meta
*.aab.meta
*.unitypackage.meta
*.app.meta

# ── Crash reports ─────────────────────────────────────────────────────────────
sysinfo.txt
crashlytics-build.properties

# ── Server build output (deploy separately via SCP) ──────────────────────────
ServerBuild/

# ── Test zips / scratch ───────────────────────────────────────────────────────
*.zip
*.zip.meta
TestDemo*

# ── Shareable builds (distribute via download page, not git) ─────────────────
Shareable Windows Build/
New folder/
Crossworlds Windows/

# ── Debug symbols ─────────────────────────────────────────────────────────────
*_BackUpThisFolder_ButDontShipItWithYourGame/
CrossWorlds/README.md

# ── Local-only Assets folders (contents already ignored above, so their folder
#    .metas must be ignored too or meta_guard flags them as stale) ─────────────
Assets/AI Toolkit/
Assets/AI Toolkit.meta
Assets/brbmuffins Trails/
Assets/brbmuffins Trails.meta

# ── Local CI/CD setup helper (one-shot, prompts for secrets — not for git) ───
tools/ci-setup.sh
_CONTEXT/VFX_VARIANT_ASSIGNMENT_PROMPT.txt.md

# ── Claude / AI session files (local only, never share) ──────────────────────
.claude/
HISTORY_SCRUB_PLAN.md

# ── Session artifacts / accidental junk ──────────────────────────────────────
dev/
*_prompt_summary.txt
tonight_*.txt
WIKI_CORRECTIONS_*.md
# Untracked leftovers of files removed from git 2026-07-25 (delete blocked in sandbox;
# safe to delete these two from disk manually):
Backend_GM_Permissions_Handoff.md
Docs/vps-character-system-prompt.txt
